suite_namespace: common\tests\unit
actor: UnitTester
bootstrap: false
modules:
    enabled:
        - Yii2:
            part: [orm, fixtures]
            transaction: false
            cleanup: false # do not cleanup the fixtures
        - Asserts